import React from "react";
import Add from 'Add';
import sub from 'sub';
import mult from 'mult'
import div from 'div';

function add(a,b){

    <div>
        <ul>
            <li>{add(40,4)}</li>
            <li>{sub(50,30)}</li>
            <li>{mult(30,2)}</li>
            <li>{div(40,20)}</li>
        </ul>
    </div>
    
    let sum=a+b;
    return sum;

    let sub=a-b;
    return sub;

    let mult=a*b;
    return mult;

    let div=a/b;
    return div;

}

export default Add;